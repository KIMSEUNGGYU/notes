# 카디널리티

> 구조 예시 문서. 자리를 보여주려고 넣었다.

`study/{주제}/개념/` 에는 작업하다 "개념이 부족하네" 싶을 때 정리한 글이 온다.
계속 갱신되는 글이라 날짜 접두사를 안 붙인다.

메트릭 하나가 가질 수 있는 라벨 조합의 수. 라벨 값이 늘어나면
시계열 개수가 곱으로 늘어난다.

- `http_requests_total{method, status}` → 메서드 5 × 상태 6 = 30 시계열
- 여기에 `user_id` 를 붙이면 사용자 수만큼 곱해진다
