{
  "name": "notes",
  "version": "1.0.0",
  "private": true,
  "description": "프론트엔드 개발 지식 및 업무 경험 문서",
  "scripts": {
    "docs:best-practices:dev": "pnpm --filter best-practices dev",
    "docs:best-practices:build": "pnpm --filter best-practices build",
    "docs:best-practices:preview": "pnpm --filter best-practices preview",
    "docs:work:dev": "pnpm --filter work-notes dev",
    "docs:work:build": "pnpm --filter work-notes build",
    "docs:work:preview": "pnpm --filter work-notes preview",
    "build": "node scripts/build.mjs",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "catalog:",
    "vitepress": "catalog:",
    "vue": "catalog:"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
